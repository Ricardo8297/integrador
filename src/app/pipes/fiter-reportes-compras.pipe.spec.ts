import { FiterReportesComprasPipe } from './fiter-reportes-compras.pipe';

describe('FiterReportesComprasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterReportesComprasPipe();
    expect(pipe).toBeTruthy();
  });
});
