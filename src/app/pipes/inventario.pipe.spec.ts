import { FiterInventarioPipe } from './inventario.pipe';

describe('InventarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiterInventarioPipe();
    expect(pipe).toBeTruthy();
  });
});
